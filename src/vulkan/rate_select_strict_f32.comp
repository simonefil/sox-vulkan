/* Select a decimated double-single rate-stage output.
 *
 * The strict profile's member of the select family; rate_select_f64.comp
 * carries the family's rationale.  Samples are double-single pairs, so the
 * normalisation goes through multiply_double_single rather than a plain
 * multiply: 1/2147483647 is not representable in FP32 either, and the pair
 * exists precisely to hold what that rounding would otherwise lose.
 *
 * Bindings, in the order run_resident_selection() in rate_vulkan.c writes
 * them:
 *   0 input_samples   -- in: the filtered block, planar, double-single pairs
 *                        as fir_partition_strict_f32.comp emitted them.
 *   1 output_samples  -- out: the decimated block, interleaved, still pairs.
 *
 * Push constants (select_parameters_t, 32 bytes): output_frames,
 * first_input_frame, input_step, input_channel_stride, channels, normalize,
 * padding1, padding2.  The strides count vec2 elements, not floats.
 *
 * Dispatch is two-dimensional, x over output_frames in workgroups of 128 and
 * y one invocation per channel, hence both bounds tests.
 */

#version 450

layout(local_size_x = 128) in;

layout(std430, binding = 0) readonly buffer InputSamples {
  vec2 values[];
} input_samples;

layout(std430, binding = 1) writeonly buffer OutputSamples {
  vec2 values[];
} output_samples;

layout(push_constant) uniform Parameters {
  uint output_frames;
  uint first_input_frame;
  uint input_step;
  uint input_channel_stride;
  uint channels;
  uint normalize;
  uint padding1;
  uint padding2;
} parameters;

const float SOX_SAMPLE_SCALE = 1.0 / 2147483647.0;

/* Scale a double-single pair by a plain float, carrying the low word.
 *
 * fma(value.x, scale, -product) is exactly what the high product lost to
 * rounding; adding value.y * scale gives the residual, and the last two steps
 * renormalise.  The `precise` locals are what keep a reassociating compiler
 * from deleting terms that are algebraically zero -- see double_double.glsl,
 * which states the same rules for the FP64 pairs. */
vec2 multiply_double_single(vec2 value, float scale)
{
  precise float product = value.x * scale;
  precise float residual = fma(value.x, scale, -product) + value.y * scale;
  precise float high = product + residual;
  precise float low = residual - (high - product);

  return vec2(high, low);
}

void main()
{
  const uint output_frame = gl_GlobalInvocationID.x;
  const uint channel = gl_GlobalInvocationID.y;

  if (output_frame >= parameters.output_frames || channel >= parameters.channels)
    return;

  const uint input_frame = parameters.first_input_frame + output_frame * parameters.input_step;
  vec2 value = input_samples.values[channel * parameters.input_channel_stride + input_frame];

  if (parameters.normalize != 0u)
    value = multiply_double_single(value, SOX_SAMPLE_SCALE);
  output_samples.values[output_frame * parameters.channels + channel] = value;
}
